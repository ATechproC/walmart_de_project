name: 'walmart_project'
version: '1.0.0'

profile: 'walmart_project'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  walmart_project:
    silver:
      silver_tech:
        +materialized: table
        +schema: silver_tech
      silver_business:
        +materialized: table
        +schema: silver_business
    gold:
      +materialized: table
      +schema: gold
      ephemeral:
        +materialized: ephemeral
